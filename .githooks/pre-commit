#!/bin/sh
# pre-commit hook: 接口文档同步检查（增量模式）
# 改动 server 源码且新增/变更了路由,但未同步写入接口文档 → 阻止提交

node scripts/check-api-docs.mjs
status=$?
if [ $status -ne 0 ]; then
  echo ""
  echo "🚫 提交被拦截: 接口变更必须同步更新 doc/api_specification.md 或 doc/api/admin.md"
  exit 1
fi
exit 0
