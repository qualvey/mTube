[x] 管理端显示当前时间。
[x] B端累计营收与订单数严格过滤只统计已完成支付(PAID)的订单。
[x] admin前端显示上传进度和网速
[x] 独立 TCP 多通道并发直传（突破 171 KB/s 提升至 6 MB/s 物理上限）
[x] 网络抖动自动重连（分片级退避重试 3 次）
[x] 断点续传（基于唯一文件特征码 + 历史分片探测）
[x] 系统设置中配置直传并发连接数 (2 - 8 通道)

现在封面加载慢。
如果后面视频变多了，现在的逻辑是不是会把所有的视频全部渲染卡片？

客户需求如下。
1. 上传队列，先选择文件，push到队列（这时候可以发到存储节点），然后指定时间，到时间才在主站显示
2. 加入搜索框，根据现有最佳实践进行（我也不知道怎么做的，评估一下吧）
3. 加入侧边栏分类功能（设计一版方案）
4. 设计广告植入模式
5. 添加详情页面，从视频流点进去， 进入详情view，类似youtube.
6. 收费模式改成可选，由管理员控制（目前的业务方向暂时放弃收费）

## 已经解决
<!-- 提出需求如下：
1. 请让容器也适配封面的比例。
2. 优化封面加载速度，确保在页面加载完，所有的card封面都准备好（至少视口往下10个预渲染）
3. 增加节流机制，后端接口对video接口返回的数量分页。前端按需拉取下一页。
非预期行为。
在封面已加载->触发激活->拉流成功并渲染。
这个过程中，中间的overlay展示的时候会恢复横屏比例。
请让overlay跟随容器的大小。 -->
